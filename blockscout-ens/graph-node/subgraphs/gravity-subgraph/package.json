{
  "name": "gravity-name-service-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ gravity-name-service-subgraph",
    "create-local": "graph create --node http://localhost:8020/ gravity-name-service-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ gravity-name-service-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 gravity-name-service-subgraph",
    "test": "graph test"
  },
  "devDependencies": {
    "@ensdomains/content-hash": "^3.0.0",
    "@graphprotocol/graph-cli": "^0.92.0",
    "@graphprotocol/graph-ts": "^0.31.0",
    "assemblyscript": "^0.19.0",
    "matchstick-as": "^0.6.0",
    "typescript": "^4.9.4"
  }
}
